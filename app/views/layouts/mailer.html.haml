!!!
%html
  %head
    %meta{ content: 'text/html; charset=utf-8', http_equiv: 'Content-Type' }
    %style
      /* Email styles need to be inline */
      /* 1-16-19: the orig, html.erb tag was <style></style> */
      /*  should this be `%style` instead of `:css` ? */

  %body
    = yield
